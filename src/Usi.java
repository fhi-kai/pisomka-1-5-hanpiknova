public class Usi implements ZajacieUsi {

    public int zajacieUsi(int pocetZajacov) {
        int pocet_usi = 0;
        for(int i=1;i++)
        { if (pocetZajacov == i);
            else if (pocetZajacov == i + 1)}
        return pocet_usi;
}
}

